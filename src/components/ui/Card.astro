---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className, as = 'article' } = Astro.props as Props;
const Tag = as;
---

<Tag class={cn('surface-card p-6 md:p-8', className)}>
  <slot />
</Tag>
