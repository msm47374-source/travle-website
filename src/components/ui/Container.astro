---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className, as = 'div' } = Astro.props as Props;
const Tag = as;
---

<Tag class={cn('site-container', className)}>
  <slot />
</Tag>
